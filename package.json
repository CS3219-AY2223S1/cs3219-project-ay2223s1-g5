{
  "name": "peerprep",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "on-backend": "npm run --prefix backend",
    "on-frontend": "npm run --prefix frontend",
    "all": "concurrently -c green,blue,yellow",
    "dev": "npm run --prefix shared build && npm run all -- --kill-others \"npm:on-* dev\"",
    "build": "npm run --prefix shared build && npm run all \"npm:on-* build\"",
    "postinstall": "npm run all \"npm --prefix backend install\" \"npm --prefix frontend install\" \"npm --prefix shared install\" && npm run --prefix shared build",
    "prepare": "husky install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/CS3219-AY2223S1/cs3219-project-ay2223s1-g5.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^17.0.3",
    "@commitlint/config-conventional": "^17.0.3",
    "concurrently": "^7.3.0",
    "husky": "^8.0.1"
  }
}
